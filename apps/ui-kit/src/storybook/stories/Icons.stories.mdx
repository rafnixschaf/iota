import { Meta, Typeset, Unstyled } from '@storybook/blocks';
import * as Icons from '@iota/icons';

<Meta title="Icons/All Icons" />

# Icons

<Unstyled>
    <div className="grid grid-cols-4 gap-x-4 gap-y-8">
        {Object.keys(Icons).map((key) => {
            const Icon = Icons[key];
            return (
                <div key={key} className="p-2 flex flex-col items-center gap-2">
                    <Icon />
                    <small>{key}</small>
                </div>
            );
        })}
    </div>
</Unstyled>
