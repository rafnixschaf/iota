{
    "name": "move-bytecode-template",
    "version": "0.1.0",
    "description": "Move Binary Format implementation in WASM",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "wasm-pack build --target nodejs --scope iota",
        "build:dev": "wasm-pack build --target nodejs --debug --scope iota",
        "build:release": "wasm-pack build --target web --release --scope iota",
        "test": "vitest run"
    },
    "author": "IOTA Foundation <contact@iota.org>",
    "license": "Apache-2.0",
    "devDependencies": {
        "@iota/bcs": "workspace:*",
        "@iota/build-scripts": "workspace:*",
        "typescript": "^5.3.3",
        "vitest": "^0.33.0",
        "wasm-pack": "^0.12.1"
    }
}
